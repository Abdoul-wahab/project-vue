# nuxt-ecv

## Build Setup

```bash
# install dependencies
$ npm install

# Copy .env file
cp .env.example .env

# start JSON Server at localhost:3000
$ npm run serve

# serve with hot reload at localhost:XXXXX
$ npm run dev

# build for production and launch server
$ npm run build
$ npm run start
